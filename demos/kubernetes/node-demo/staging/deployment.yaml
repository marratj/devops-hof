---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: devops-demo-staging
  annotations:
    flux.weave.works/automated: "true"
    flux.weave.works/tag.devops-demo: "glob:*"
  labels:
    app: devops-demo
    environment: staging
spec:
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: devops-demo
        environment: staging
    spec:
      containers:
      - image: gcr.io/devops-hof/node-demo:c7cb2ed5d4d47764b3f4beda0224de9377ab37cf
        name: devops-demo
        resources:
          limits:
            cpu: "100m"
            memory: "200Mi"
        ports:
        - containerPort: 3000
          name: nodejs
      restartPolicy: Always
