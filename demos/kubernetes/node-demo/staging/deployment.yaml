---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: devops-demo-staging
  annotations:
    flux.weave.works/automated: "false"
    flux.weave.works/tag.devops-demo: "glob:*"
  labels:
    app: devops-demo
    environment: staging
spec:
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: devops-demo
        environment: staging
    spec:
      containers:
      - image: gcr.io/devops-hof/node-demo:c3393c936cd7e63c7df03e3f8d5b946a699960e8
        name: devops-demo
        resources:
          limits:
            cpu: "100m"
            memory: "200Mi"
        ports:
        - containerPort: 3000
          name: nodejs
      restartPolicy: Always
