---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: devops-demo-staging
  annotations:
    flux.weave.works/automated: "true"
    flux.weave.works/tag.devops-demo: "glob:*"
  labels:
    app: devops-demo
    environment: staging
spec:
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: devops-demo
        environment: staging
    spec:
      containers:
      - image: gcr.io/devops-hof/node-demo:54a270edb6642966b90dff33d99c50594e7a602d
        name: devops-demo
        resources:
          limits:
            cpu: "100m"
            memory: "200Mi"
        ports:
        - containerPort: 3000
          name: nodejs
      restartPolicy: Always
